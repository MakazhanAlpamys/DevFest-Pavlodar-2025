# DevFest Pavlodar 2025 - RAG City Advisor
## 🚀 Краткая инструкция по запуску

---

## Быстрый старт за 5 минут

### 1. Установка зависимостей

**Backend:**
```bash
cd backend
npm install
```

**Frontend:**
```bash
cd frontend
npm install
```

---

### 2. Настройка Gemini API

1. Получите **бесплатный** API ключ на https://makersuite.google.com/app/apikey
2. Создайте файл `backend/.env`:

```env
GEMINI_API_KEY=ваш_ключ_здесь
PORT=3001
```

**⚠️ Важно:** Не коммитьте `.env` файл в Git!

---

### 3. Запуск приложения

**Terminal 1 - Backend:**
```bash
cd backend
npm start
```

**Terminal 2 - Frontend:**
```bash
cd frontend
npm run dev
```

---

### 4. Открытие в браузере

Откройте: **http://localhost:5173**

✅ Разрешите доступ к геолокации для лучших рекомендаций!

---

## 📝 Примеры запросов

Попробуйте эти запросы после запуска:

```
✨ "Хочу прогуляться 30-60 минут, что посоветуете?"
✨ "Ищу тихое кафе с Wi-Fi для работы"
✨ "Где рядом есть детские площадки?"
✨ "Что можно посмотреть за 2 часа?"
✨ "Посоветуй ресторан для группы из 8 человек"
✨ "Найди спортзал с доступной средой"
```

---

## 🔧 Решение проблем

### Backend не запускается:
- ✅ Проверьте наличие `.env` файла в папке `backend/`
- ✅ Убедитесь, что `GEMINI_API_KEY` корректный
- ✅ Проверьте, что порт `3001` свободен

**Проверка порта:**
```bash
# Windows
netstat -ano | findstr :3001

# Linux/Mac
lsof -i :3001
```

---

### Frontend не подключается:
- ✅ Убедитесь, что backend запущен на http://localhost:3001
- ✅ Проверьте консоль браузера (F12) на ошибки CORS
- ✅ Очистите кэш браузера (Ctrl+Shift+Del)

---

### Геолокация не работает:
- ✅ Используйте HTTPS или localhost (HTTP не поддерживается для геолокации)
- ✅ Проверьте настройки браузера (разрешения на геолокацию)
- ✅ Система работает и без геолокации! (но без расстояний)

---

### Vector Store не инициализируется:
- ✅ Проверьте, что `GEMINI_API_KEY` установлен
- ✅ Дождитесь первой инициализации (~30-60 секунд для 500 POI)
- ✅ Проверьте статус: `GET http://localhost:3001/api/rag/status`

**Принудительная инициализация:**
```bash
curl -X POST http://localhost:3001/api/rag/init-vector-store \
  -H "Content-Type: application/json" \
  -d '{"forceRebuild": true, "maxPOIs": 500}'
```

---

## 📊 API Endpoints для тестирования

### Health Check
```bash
curl http://localhost:3001/api/health
```

### RAG Status
```bash
curl http://localhost:3001/api/rag/status
```

### Test Semantic Search
```bash
curl -X POST http://localhost:3001/api/rag/test-search \
  -H "Content-Type: application/json" \
  -d '{"query": "тихое кафе с Wi-Fi", "topK": 5}'
```

### Get Recommendations
```bash
curl -X POST http://localhost:3001/api/recommend \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Хочу прогуляться 30 минут",
    "location": {"latitude": 51.1694, "longitude": 71.4491},
    "filters": {"category": "all", "radius": 5}
  }'
```

---

## 🏗️ Структура проекта

```
DevFest-Pavlodar-2025/
├── backend/                 # Node.js + Express + RAG
│   ├── index.js            # Главный файл с RAG logic
│   ├── package.json        # Dependencies
│   ├── .env               # API ключи (не коммитится)
│   └── scrapers/          # Event scrapers
│
├── frontend/                # React + TypeScript + Tailwind
│   ├── src/
│   │   ├── App.tsx        # Главный компонент
│   │   ├── components/    # UI компоненты
│   │   └── i18n.ts       # Мультиязычность
│   ├── public/
│   │   └── gis.csv       # База 124K+ POI
│   └── package.json
│
├── README.md               # Полная документация
├── QUICKSTART.md          # Этот файл
├── GIT_SETUP.md           # Инструкция по Git
└── тз.md                 # Техническое задание
```

---

## 📚 Дополнительная информация

- **Полная документация:** [README.md](./README.md)
- **Техническое задание:** [тз.md](./тз.md)
- **Настройка Git:** [GIT_SETUP.md](./GIT_SETUP.md)

---

## 🎯 DevFest Pavlodar 2025

Этот проект создан для хакатона **DevFest Pavlodar 2025** и демонстрирует:

✅ **RAG-архитектура** (Retrieval-Augmented Generation)  
✅ **Gemini Embeddings** (векторизация данных)  
✅ **Semantic Search** (поиск по смыслу)  
✅ **Прозрачность источников** (показ извлечённых фрагментов)  
✅ **Защита от галлюцинаций** (проверка контекста)  

---

**Happy coding! 🚀**

Made with ❤️ for DevFest Pavlodar 2025
